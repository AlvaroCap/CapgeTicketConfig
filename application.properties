# info JPA
spring.jpa.show-sql=true
# retrasar la inicialización de la BBDD hasta que exista
spring.jpa.defer-datasource-initialization=true

# (Servidor EUREKA)
eureka.client.serviceUrl.defaultZone=http://localhost:1111/eureka/

# Si se indica a false no funciona DevTools. TRUE por defecto
spring.devtools.restart.enabled=true

#Permite conocer los endpoints
logging.level.org.springframework.web.servlet.mvc.method.annotation=TRACE

#Configuración DB
spring.datasource.url=jdbc:mysql://10.80.150.107:3306/capgeticketdb
#spring.datasource.url=jdbc:mysql://localhost:3306/capgeticketdb
spring.datasource.username=root
spring.datasource.password=rootroot
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
